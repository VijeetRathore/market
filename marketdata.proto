syntax = "proto3";

message FeedResponse {
  map<string, Feed> feeds = 1;
}

message Feed {
  LTPC ltpc = 1;
  MarketLevel marketLevel = 2;
}

message LTPC {
  double ltp = 1;
  int64 ltt = 2;
  int64 ltq = 3;
}

message MarketLevel {
  int64 oi = 1;
  int64 volume = 2;
}